[project]
name = "goated-prompter"
version = "1.0.1"
description = "Local prompt-generation website with a shared Python engine and optional ComfyUI integration."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
dependencies = []

[project.urls]
Repository = "https://github.com/Jolanoff/goated-prompter"
Issues = "https://github.com/Jolanoff/goated-prompter/issues"

[tool.setuptools]
py-modules = []

[tool.setuptools.packages.find]
include = ["goated_prompter*"]
exclude = ["tests*", "docs*", "assets*"]
